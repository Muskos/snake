import { Position } from "src/types";

export const DEFAULT_SNAKE: Position[] = [{ x: 10, y: 10 }];
export const DEFAULT_SPECIAL_FOOD_CHANCE = 0.2;
export const DEFAULT_BASE_SPEED = 1;
export const DEFAULT_MAX_SPEED = 5;